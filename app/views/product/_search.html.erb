<%= form_with(url: search_index_path, local: true, method: "get") do %>
  <%= select_tag "category", options_from_collection_for_select(Category.all, "id", "name") %>
  <%= text_field_tag(:search_term) %>
  <%= submit_tag("Search", class: 'search-button') %>
<% end %>